include:
  - common.yml

services:
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_cpu_data:/root/.ollama
    restart: always
    networks:
      - kuab-network

volumes:
  ollama_cpu_data:
